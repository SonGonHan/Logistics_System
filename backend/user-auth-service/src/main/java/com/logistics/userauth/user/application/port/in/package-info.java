/**
 * Inbound ports (use cases) модуля пользователя.
 *
 * <p>Содержит интерфейсы, описывающие сценарии использования (application layer),
 * которые вызываются из входных адаптеров (например, REST контроллеров).
 *
 * <p>Реализации этих интерфейсов располагаются в пакете {@code com.logistics.userauth.user.application.usecase}.
 */
package com.logistics.userauth.user.application.port.in;

/**
 * Реализации use case'ов (application services) модуля пользователя.
 *
 * <p>Содержит бизнес-логику сценариев использования, определённых в inbound ports
 * пакета {@code com.logistics.userauth.user.application.port.in}.
 *
 * <p>Данный слой работает с доменной моделью и репозиториями (ports.out),
 * а также преобразует результат в DTO ответа через мапперы, используемые контроллерами.
 */
package com.logistics.userauth.user.application.usecase;
